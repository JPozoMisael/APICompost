version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env  # Esto carga automáticamente todas las variables del archivo
    volumes:
      - .:/var/www/html
